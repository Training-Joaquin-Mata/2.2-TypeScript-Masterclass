{
  /* El orden en el que se ejecutasn es files, includes y exclude, con file compila el archivo que declaras, con includes todos los que quieras y exclude excluye los que quieras*/
  // "files": ["src/app.spec.ts"],
  // "includes":["src/**/*.ts"],
  // "exclude": ["src/**/*.spec.ts"],
  "compilerOptions": {
    "target": "es5",                                 
    "module": "commonjs",
    //"module": "system",      
     "lib": ["dom", "es6"],
     "outDir": "dist",
    // "outFile": "dist/bundle",
    // no crea ek aoo,js cuando encuentra un error
   // "noEmitOnError": true,
   // "noImplicitAny": true,
  //  "sourceMap": true,
  //  "inlineSources": true,
  //  "mapRoot": "/mappings/"
  //  "sourceMap": true,
      // "experimentalDecorators": true,
      // "emitDecoratorMetadata": true,
  }
}
